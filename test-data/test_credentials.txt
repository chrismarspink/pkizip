[시나리오 5.1] 자격증명 외부 전송 차단 OPA 룰 검증

→ 그대로 .txt 사용 (변환 X)
→ 분석 → 가명화 건너뛰기 → 의도 "외부" 선택
→ "DPV_CREDENTIAL_EXTERNAL_BLOCKED" 차단 메시지 확인

배포 환경 설정값

운영팀에서 사용하는 클라우드 인증 정보입니다.
실수로 GitHub 에 push 안 되도록 주의 부탁드립니다.

AWS 액세스 정보:
  AWS_ACCESS_KEY_ID = AKIAQRSTUVWXYZ123456
  AWS_ACCESS_KEY_ID = AKIA1234567890ABCDEF
  AWS_SECRET_ACCESS_KEY = (별도 저장소 참조)
  Region = ap-northeast-2

API 키 (사내 시스템):
  GENERIC_API_KEY = sk-abc123XYZ456def789ghi012JKL345mn678
  PROD_API_KEY = pk-prod-9876543210abcdef0123456789

서버 SSH 키:
  /home/deploy/.ssh/id_rsa (별도 보관)

데이터베이스 접속 정보:
  Host: db.internal.example.com
  Port: 5432
  User: app_admin
  Password: (별도 저장소 참조)

본 파일은 절대 외부 전송 금지 — PKIZIP 이 자동으로 차단합니다.
